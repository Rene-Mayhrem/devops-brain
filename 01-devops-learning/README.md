# DevOps Learning — Index

This folder contains concept notes, project logs, and focused subfolders. Use this file as a quick map for what's inside and where to look first.

Subfolders:

- `ci-cd/` — Continuous integration and delivery notes and examples.
- `docker/` — Dockerfile examples, images, and container patterns.
- `kubernetes/` — Cluster, manifests, and learning notes.
- `observability/` — Metrics, logging, and tracing notes.
- `terraform/` — IaC examples and patterns.
- `projects/` — project templates and mini-project logs.
- `glossary.md` — quick reference of terms.

Recommendations

- Add a one-line summary to major notes (first line) so `rg` and index scans surface useful summaries.
- Use YAML frontmatter with `tags` and `date` fields for programmatic filtering.
- Keep project logs under `projects/` with a short README per project.

If you want, I can generate a short index of files and first-line summaries for this folder.
